================================================================================   
ZMapApp

The code that forms the "main" of the zmap client application, it constructs
the initial zmap window and creates/destroys individual ZMaps.
